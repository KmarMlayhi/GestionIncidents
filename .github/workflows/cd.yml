name: CD

on:
  push:
    branches: ["main"]

permissions:
  contents: read
  packages: write

jobs:
  build_push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      - name: Build & test
        run: mvn -B clean test

      - name: Login GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository }}:v${{ github.run_number }}
            ghcr.io/${{ github.repository }}:${{ github.sha }}

  deploy:
    needs: build_push
    runs-on: self-hosted
    permissions:
      contents: read
      packages: read
    steps:
      - name: Login GHCR (on VM)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Update repo on VM
        run: |
          cd /opt/GestionIncidents
          git pull

      - name: Deploy
        env:
          APP_IMAGE: ghcr.io/${{ github.repository }}:v${{ github.run_number }}
        run: |
          cd /opt/GestionIncidents
          chmod +x deploy.sh
          APP_IMAGE="$APP_IMAGE" ./deploy.sh